{
  "name": "twitter-blocker",
  "version": "1.3.7",
  "description": "ポップアップで指定した時間だけ Twitter のブロックを解除できる拡張機能",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "dev:reload": "npm run build && concurrently \"npm run dev\" \"web-ext run --source-dir dist --target chromium --start-url x.com\"",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "concurrently": "^9.2.1",
    "esbuild": "^0.27.2",
    "typescript": "^5.3.3",
    "web-ext": "^9.2.0"
  },
  "dependencies": {
    "canvas": "^3.1.2"
  }
}
