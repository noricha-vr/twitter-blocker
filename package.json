{
  "name": "twitter-blocker",
  "version": "1.3.2",
  "description": "ポップアップで指定した時間だけ Twitter のブロックを解除できる拡張機能",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "watch": "tsc --watch & npm run copy-assets --watch",
    "dev": "tsc --watch --sourceMap & npm run copy-assets",
    "clean": "rm -rf dist",
    "copy-assets": "cp src/*.html dist/ 2>/dev/null || true && cp src/*.css dist/ 2>/dev/null || true"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "canvas": "^3.1.2"
  }
}
